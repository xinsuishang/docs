# 新建github项目
---

```bash
// 使用git管理
$ git init
// 作者
$ git config user.name YOUR_NAME
$ git config user.email YOUR_EMAIL
// 绑定远程分支
$ git remote add origin YOUR_GIT_ADDR
// 合并远程分支到本地
$ git pull --rebase origin master
// 将本地内容推送(-u 为指定默认主机并关联 以便于以后推送和拉取时简化命令)
$ git push -u origin maste
```
